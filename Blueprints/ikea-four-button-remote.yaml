blueprint:
  name: IKEA four button remote
  description:
    'Control lights with an IKEA four button remote (the square ones),
    for use with [ZHA](https://www.home-assistant.io/integrations/zha/).


    Extends [SwerveShot](https://community.home-assistant.io/u/SwerveShot)''s [version](https://community.home-assistant.io/t/zha-ikea-four-button-remote-for-lights/347657)
    which is based on the work of [frenck](https://community.home-assistant.io/u/frenck)
    with the five button version.


    Pressing the up button will turn on the lights at the last set brightness (unless
    the force brightness is toggled on in the blueprint).


    Pressing the down button will turn the lights off again.


    Pressing and holding the up/down buttons will change the brightness smoothly and
    can be pressed and hold until the brightness is satisfactory.


    The "left" and "right" buttons can be assigned to a short and long button press
    action. This allows you to assign, e.g., a scene or anything else.


    Alternatively, enable color cycling mode to use the left button to cycle through
    colors in one direction and the right button to cycle in the opposite direction.
    This works with color-capable lights only.

    '
  domain: automation
  input:
    remote:
      name: Remote
      description: IKEA remote to use
      selector:
        device:
          integration: zha
          manufacturer: IKEA of Sweden
          model: Remote Control N2
          multiple: false
    light:
      name: Light(s)
      description: The light(s) to control
      selector:
        target:
          entity:
            - domain:
                - light
    force_brightness:
      name: Force turn on brightness
      description:
        Force the brightness to the set level below, when the "on" button
        on the remote is pushed and lights turn on.
      default: false
      selector:
        boolean: {}
    brightness:
      name: Brightness
      description: Brightness of the light(s) when turning on
      default: 50
      selector:
        number:
          min: 0.0
          max: 100.0
          mode: slider
          step: 1.0
          unit_of_measurement: "%"
    hold_time:
      name: Hold Time
      description:
        Time between the execution of the brighter/darker actions when
        holding the brighter/darker button.
      default: 0.5
      selector:
        number:
          min: 0.1
          max: 2.0
          step: 0.1
          unit_of_measurement: seconds
          mode: slider
    enable_color_cycling:
      name: Enable color cycling
      description:
        Enable color cycling mode for left/right buttons. When enabled, short
        press left cycles colors forward, short press right cycles colors backward.
        Long press actions will still work. Only works with color-capable lights.
      default: false
      selector:
        boolean: {}
    color_list:
      name: Color cycling list
      description:
        List of colors to cycle through (only used when color cycling is enabled).
        Options are red, orange, yellow, green, cyan, blue, purple, pink, white, warm_white.
      default:
        - red
        - orange
        - yellow
        - green
        - cyan
        - blue
        - purple
        - pink
      selector:
        select:
          multiple: true
          options:
            - label: Red
              value: red
            - label: Orange
              value: orange
            - label: Yellow
              value: yellow
            - label: Green
              value: green
            - label: Cyan
              value: cyan
            - label: Blue
              value: blue
            - label: Purple
              value: purple
            - label: Pink
              value: pink
            - label: White
              value: white
            - label: Warm White
              value: warm_white
    button_left_short:
      name: Left button - short press
      description: Action to run on short left button press
      default: []
      selector:
        action: {}
    button_left_long:
      name: Left button - long press
      description:
        Action to run on long left button press. Use with care, as before
        the long press event is sent, the "on" event gets triggered.
      default: []
      selector:
        action: {}
    button_right_short:
      name: Right button - short press
      description: Action to run on short right button press
      default: []
      selector:
        action: {}
    button_right_long:
      name: Right button - long press
      description:
        Action to run on long right button press. Use with care, as before
        the long press event is sent, the "on" event gets triggered.
      default: []
      selector:
        action: {}
  source_url: https://community.home-assistant.io/t/zha-ikea-four-button-remote-styrbar-for-lights-e2001-e2002/384482
mode: restart
max_exceeded: silent
variables:
  force_brightness: !input force_brightness
  enable_color_cycling: !input enable_color_cycling
  color_list: !input color_list
  light_target: !input light
  color_map:
    red: [255, 0, 0]
    orange: [255, 165, 0]
    yellow: [255, 255, 0]
    green: [0, 255, 0]
    cyan: [0, 255, 255]
    blue: [0, 0, 255]
    purple: [128, 0, 128]
    pink: [255, 192, 203]
    white: [255, 255, 255]
    warm_white: [255, 220, 180]
trigger:
  - platform: event
    event_type: zha_event
    event_data:
      device_id: !input remote
action:
  - variables:
      command: "{{ trigger.event.data.command }}"
      cluster_id: "{{ trigger.event.data.cluster_id }}"
      endpoint_id: "{{ trigger.event.data.endpoint_id }}"
      args: "{{ trigger.event.data.args }}"
  - choose:
      - conditions:
          - "{{ command == 'on' }}"
          - "{{ cluster_id == 6 }}"
          - "{{ endpoint_id == 1 }}"
        sequence:
          - choose:
              - conditions: "{{ force_brightness }}"
                sequence:
                  - service: light.turn_on
                    target: !input light
                    data:
                      transition: 1
                      brightness_pct: !input brightness
            default:
              - service: light.turn_on
                target: !input light
                data:
                  transition: 1
      - conditions:
          - "{{ command == 'off' }}"
          - "{{ cluster_id == 6 }}"
          - "{{ endpoint_id == 1 }}"
        sequence:
          - service: light.turn_off
            target: !input light
            data:
              transition: 1
      - conditions:
          - "{{ command == 'move_with_on_off' }}"
          - "{{ cluster_id == 8 }}"
          - "{{ endpoint_id == 1 }}"
        sequence:
          - repeat:
              count: 10
              sequence:
                - service: light.turn_on
                  target: !input light
                  data:
                    brightness_step_pct: 10
                    transition: !input hold_time
                - delay: !input hold_time
      - conditions:
          - "{{ command == 'move' }}"
          - "{{ cluster_id == 8 }}"
          - "{{ endpoint_id == 1 }}"
        sequence:
          - repeat:
              count: 10
              sequence:
                - service: light.turn_on
                  target: !input light
                  data:
                    brightness_step_pct: -10
                    transition: !input hold_time
                - delay: !input hold_time
      - conditions:
          - "{{ command == 'press' }}"
          - "{{ cluster_id == 5 }}"
          - "{{ endpoint_id == 1 }}"
          - "{{ args == [257, 13, 0] }}"
        sequence:
          - choose:
              - conditions: "{{ enable_color_cycling }}"
                sequence:
                  - variables:
                      light_entity: >
                        {% set entities = expand(light_target.entity_id) | map(attribute='entity_id') | list if light_target.entity_id is defined
                           else expand(light_target) | map(attribute='entity_id') | list %}
                        {{ entities[0] if entities | length > 0 else '' }}
                      current_rgb: "{{ state_attr(light_entity, 'rgb_color') | default([255, 255, 255]) }}"
                      current_index: >
                        {% set current = current_rgb | list %}
                        {% set found = namespace(index=0, min_distance=999999) %}
                        {% for color in color_list %}
                          {% set target = color_map[color] %}
                          {% set distance = ((current[0] - target[0]) ** 2 + (current[1] - target[1]) ** 2 + (current[2] - target[2]) ** 2) ** 0.5 %}
                          {% if distance < found.min_distance %}
                            {% set found.min_distance = distance %}
                            {% set found.index = loop.index0 %}
                          {% endif %}
                        {% endfor %}
                        {{ found.index }}
                      next_index: "{{ (current_index | int + 1) % (color_list | length) }}"
                      next_color: "{{ color_list[next_index | int] }}"
                      next_rgb: "{{ color_map[next_color] }}"
                  - service: light.turn_on
                    target: !input light
                    data:
                      rgb_color: "{{ next_rgb }}"
                      transition: 0.5
            default: !input button_left_short
      - conditions:
          - "{{ command == 'hold' }}"
          - "{{ cluster_id == 5 }}"
          - "{{ endpoint_id == 1 }}"
          - "{{ args == [3329, 0] }}"
        sequence: !input button_left_long
      - conditions:
          - "{{ command == 'press' }}"
          - "{{ cluster_id == 5 }}"
          - "{{ endpoint_id == 1 }}"
          - "{{ args == [256, 13, 0] }}"
        sequence:
          - choose:
              - conditions: "{{ enable_color_cycling }}"
                sequence:
                  - variables:
                      light_entity: >
                        {% set entities = expand(light_target.entity_id) | map(attribute='entity_id') | list if light_target.entity_id is defined
                           else expand(light_target) | map(attribute='entity_id') | list %}
                        {{ entities[0] if entities | length > 0 else '' }}
                      current_rgb: "{{ state_attr(light_entity, 'rgb_color') | default([255, 255, 255]) }}"
                      current_index: >
                        {% set current = current_rgb | list %}
                        {% set found = namespace(index=0, min_distance=999999) %}
                        {% for color in color_list %}
                          {% set target = color_map[color] %}
                          {% set distance = ((current[0] - target[0]) ** 2 + (current[1] - target[1]) ** 2 + (current[2] - target[2]) ** 2) ** 0.5 %}
                          {% if distance < found.min_distance %}
                            {% set found.min_distance = distance %}
                            {% set found.index = loop.index0 %}
                          {% endif %}
                        {% endfor %}
                        {{ found.index }}
                      prev_index: "{{ (current_index | int - 1) % (color_list | length) }}"
                      prev_color: "{{ color_list[prev_index | int] }}"
                      prev_rgb: "{{ color_map[prev_color] }}"
                  - service: light.turn_on
                    target: !input light
                    data:
                      rgb_color: "{{ prev_rgb }}"
                      transition: 0.5
            default: !input button_right_short
      - conditions:
          - "{{ command == 'hold' }}"
          - "{{ cluster_id == 5 }}"
          - "{{ endpoint_id == 1 }}"
          - "{{ args == [3328, 0] }}"
        sequence: !input button_right_long
